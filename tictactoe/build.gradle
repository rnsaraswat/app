// Top-level build file
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:9.1.1'   // ✅ सही
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}